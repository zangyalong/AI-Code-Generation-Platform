spring:
  application:
  name: AI-Code-Generation-Platform
  # mysql
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/acgp?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: root
    password: z200209255

server:
  port: 8123
  servlet:
    context-path: /api
# springdoc-openapi
springdoc:
  group-configs:
    - group: 'default'
      packages-to-scan: com.zangyalong.aicodegenerationplatform.controller
# knife4j
knife4j:
  enable: true
  setting:
    language: zh_cn
